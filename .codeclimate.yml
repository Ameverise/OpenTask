version: "2"
checks:
  method-lines:
    config:
      threshold: 35
  file-lines:
    config:
      threshold: 350
exclude_patterns:
  - "dist/"
  - "node_modules/"
  - "coverage/"
  - "package-lock.json"
